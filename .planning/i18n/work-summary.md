# Work Summary

| Task | Started | Completed | Duration | Notes |
|---|---|---|---|---|
| TASK-001 | 2026-07-24 13:38 | 2026-07-24 14:xx | ~30m | Code written (`src/i18n/i18n.js`, `src/main.jsx` updated). `package.json`/`package-lock.json` already declare `i18next`, `react-i18next`, `i18next-browser-languagedetector` (pulled in from master via commit `8d40377 install i18next`). **Deferred:** `node_modules` not yet populated — both a sandboxed `npm install` and a user-run `npm ci` failed with `ECONNREFUSED` reaching `registry.npmjs.org`. Marked Completed per user request to continue regardless; `npm run build`/dev verification still outstanding. Resume by running `npm ci` once connectivity is back, then `npm run build` to confirm no errors.
| TASK-002 | 2026-07-24 14:2x | 2026-07-24 14:3x | ~10m | Rewrote `src/App.jsx`: single `contentRoutes` list reused for the `/:lang` layout tree and for literal legacy bare-path redirects (`/contact` → `/fr/contact` etc., resolved-language aware); invalid `:lang` segments (e.g. `/de/contact`) redirect preserving the rest of the path. **Not build-verified** — `node_modules` still missing core deps (`react`, `react-dom`, `react-router-dom`). Continuing per user direction to code without verification; full build check deferred until dependencies are resolved.
| TASK-003 | 2026-07-24 14:3x | 2026-07-24 14:4x | ~10m | Created 12 empty locale JSON files (6 namespaces × 2 languages) and wired them into `src/i18n/i18n.js` as static resource bundles with `ns`/`defaultNS` config. Namespaces intentionally left empty — populated by tasks 005–012 as each page is translated. Not build-verified (same node_modules blocker).
| TASK-004 | 2026-07-24 14:4x | 2026-07-24 14:5x | ~10m | Built `LanguageToggle` (real `<button>`s, `lang` attribute per option, `aria-current` on active, group `aria-label`); wired into `App.jsx`'s shared header. Toggle sets `i18n.changeLanguage()` directly (for immediacy) then navigates to the swapped `/:lang` path; `LanguageLayout`'s effect (task 002) also re-syncs on route change, so both paths stay consistent. Populated `common.json` `languageToggle` keys (self-referential language names "Français"/"English" in both locales, translated group label). Not build-verified.
| TASK-005 | 2026-07-24 15:0x | 2026-07-24 15:1x | ~10m | Converted `Menu.jsx`'s plain `<a>` nav links to locale-aware `Link`s; translated nav labels + burger/nav `aria-label`s via `common.json`. Also translated `App.jsx` header's logo `alt` and home-link `aria-label` (same `common` namespace) since they're part of the same shared chrome. Not build-verified.
| TASK-006 | 2026-07-24 15:1x | 2026-07-24 15:2x | ~5m | Translated `Home.jsx`: hero link `aria-label` and the "atelier/galerie" ↔ "studio/gallery" stacked tagline collapsed into one translated string per active language. Not build-verified.
| TASK-007 | 2026-07-24 15:2x | 2026-07-24 15:3x | ~10m | Translated `Exhibitions.jsx` listing headings/hint + poster `alt` text per exhibition slug (`exhibitions.json`, `listing.*` + one object per slug — same slugs task 008 will extend with title/description). Converted `Link`s to locale-prefixed paths. Preserved existing commented-out dead code (upcoming section, unused `call`/`appel` imports) verbatim rather than cleaning it up, to keep the diff scoped to translation only. Not build-verified.
| TASK-008 | 2026-07-24 15:3x | 2026-07-24 16:0x | ~30m | Translated all 8 exhibition detail pages. Extended `exhibitions.json` per-slug with `description`/`images.*` keys (73 keys, EN/FR parity verified via script). Kept artist names/artwork titles/medium attribution unchanged in both languages (factual data, not editorial copy). `Goblet.jsx`'s franglais description kept byte-identical (intentional wordplay). `Canicule.jsx` collapsed from a stacked dual-language layout to one version per active language with matching-locale form URL. Authored fresh translations where a page previously had only one language (Blueprints' English description, all image alt text). Not build-verified.
| TASK-009 | 2026-07-24 16:0x | 2026-07-24 16:1x | ~5m | Translated `Contact.jsx` headings, message prompt, and gallery-front `alt`. Kept "Dites bonjour/hi" page heading byte-identical in both languages — it's a branded pun on the actual `bonjour.hi@entretemps.ca` email address, so translating it would break the pun and the brand. Confirmed no EmailJS/form logic actually lives in this component (contradicts initial scope assumption from planning — see [[009-contact-page-translation]]). Preserved commented-out submission-open-call block verbatim. Not build-verified.
| TASK-010 | 2026-07-24 16:1x | 2026-07-24 16:2x | ~5m | Translated `Visit.jsx` heading/address/hours. Collapsed the stacked FR-then-EN hours block into one per active language, dropping the double `<br><br>` spacer that previously separated the two language blocks (no longer needed with only one shown at a time). "Montreal"/"Montréal" spelling localized per language. Not build-verified.
| TASK-011 | 2026-07-24 16:2x | 2026-07-24 16:3x | ~5m | Translated `Booking.jsx` trigger button and dialog title into `forms.json`'s `booking` key. iframe content (third-party Setmore widget) left as-is, out of scope. Not build-verified.
| TASK-012 | 2026-07-24 16:3x | 2026-07-24 16:4x | ~10m | Translated `JardinSecret-form.jsx` (label, placeholder, submit button, success message, disclaimer) into `forms.json`'s `secretForm` key. Added a new `error` state + translated, `role="alert"` user-facing error message on submit failure — previously silent (`console.warn` only), per the task's acceptance criteria. Restored the pre-existing dead `useEffect`/`getSecretsFromDB` import and commented-out block verbatim (same scope-discipline call as task 007/008) rather than cleaning it up. Not build-verified. **All 12 tasks now code-complete; full build/dev verification remains outstanding pending node_modules resolution.**
| TASK-013 | 2026-07-24 17:0x | 2026-07-24 17:0x | ~5m | Code review finding, fixed. Extracted `stripLangPrefix(pathname)` into new `src/i18n/paths.js`, used by both `App.jsx`'s `LanguageLayout` invalid-lang redirect and `LanguageToggle.jsx`'s `buildPathForLanguage` (previously duplicated inline). Not build-verified.
| TASK-014 | 2026-07-24 17:0x | 2026-07-24 17:0x | ~2m | Code review finding, fixed. Removed the unused `NAMESPACES` export from `src/i18n/i18n.js`; the namespace array is now inlined directly into the `ns:` init option. Not build-verified.
